# Unleash the power of vector search and multimodal embeddings in BigQuery

**Speaker(s):** Jeff Nelson, Márton Kodok · **Channel:** Google Cloud Tech · **Date:** 2024-07-01
**Watch:** https://youtu.be/B-0dZGJDtJw · **Format:** Talk · **Level:** Intermediate
**Topics:** LLM Fundamentals, Backend/Infra

## TL;DR

BigQuery allows you to generate multimodal embeddings and perform vector searches directly on your data without complex preprocessing steps. Simplify the process of finding relevant data, identifying patterns and trends, and clustering similar objects together. Learn how to generate embeddings using familiar BigQuery SQL syntax with multimodal inputs (text, images, audio). We ll then review how to use BigQuery s vector search capabilities to explore data in new and innovative ways, leading to faster decision-making and improved insights.

## Contents

- [Architectural Overview and Core Problem Space in Unleash the power of vector search and multim](#architectural-overview-and-core-problem-space-in-unleash-the-power-of-vector-search-and-multim)
- [System Capabilities, Implementation Details, and Workload Optimization](#system-capabilities-implementation-details-and-workload-optimization)
- [Performance Scaling, Distributed Coordination, and Reliability](#performance-scaling-distributed-coordination-and-reliability)
- [Production Best Practices, Real-World Use Cases, and Next Steps](#production-best-practices-real-world-use-cases-and-next-steps)

---

## Architectural Overview and Core Problem Space in Unleash the power of vector search and multim

[Music] hello hello welcome everybody uh thank you all for attending this session on multimodal em uh multimodal vector embeddings and Vector search in big query U my name is Jeff and I am a developer advocate here at Google cloud and I need to press the right uh I specialize in data analytics and I spend a lot of my time working with big query and its integration with our other analytic Services hello I'm Marton I'm a champion innovator and certified gde I work as a software engineer at a Romanian company and I work with big querry for like 10 years awesome thanks Martin so today we'll begin with a brief background on Vector embeddings what they are uh and then we'll move to multimodal Vector embeddings so we're moving beyond just text then we'll discuss Vector search and discuss how to find relevant information using these embeddings in big query so this is all big query where we'll stay within that context uh then we'll shift gears a bit and talk about architectures some common architectural patterns that we're seeing and then we'll show uh two live demos the first being an application that Martin will show about the power of multimodal search using text and images and then in the second I'll go into a demo of Lang chain with big query using retrieval augmented generation we'll end with a slide that contains a ton of uh a ton of links for you so like you can check it out you can click on those uh it's all publicly available so check that out at the end uh so we'll just begin with multimodal embeddings what they are in general and how they work in big query so just to set the stage and take a quick peek behind the curtain of what we use like applications that we use every day chances are that many of them rely on some sort of keyword search you input a keyword some databases you know you retrieve something from a database and then you get some information back and this is foundational it works really well for a lot of applications but when we think about the sheer volume of different types of and the different types of data that we're getting in now text images video audio Etc these traditional databases start to find limitations so things like nuances how do we how do we search when we're not we can't just use a certain keyword but there are synonyms or how do we process unstructured data like images and this is what we'll be talking about today how embeddings in Vector search transform the way that we uh retrieve and interact with our information so to understand complex data like free form text we need a standard way to represent it and machine learning models can take our unstructured inputs and transform them into numerical vectors these Vector embeddings capture the underlying meaning behind your data making it possible to compare and search based on semantic relationships not just straight keywords and this opens up new possibilities like we could find pictures using just some text or we could find a customer support ticket that is similar to a certain you know we we could have an input customer support ticket and find ones that are similar even if the verbiage differs a little bit now traditionally we think about Vector embeddings on textual data so we take text we feed it through some sort of model and we get an embedding out which is just an array of numbers uh this allows us to search text to text without needing exact keywords again we're moving a little bit beyond keyword search and we can search based on their meaning so this is a semantic search now let's take an example everyone like visualize in your head the word bug like B ug bug up there what is it mean like think about it in your head what is a bug and now that you have a picture in your head consider a sentence like I found a bug in the Garden today or and like here the bug clearly refers ref to an insect like a creepy crawly insect that's crawling around your garden but what about I found a bug in my code or the lights up here are bugging me or something like that the context completely changes the meaning of the word bug and this is what Vector embeddings help with we can turn unstructured data like text into numerical representations that machines understand and we can capture the relationships between these words based on their context now we're not just confined to text any longer and this is what we'll be talking about today we can generate embeddings on multimodal data this means that text image search uh text images audio video uh can all be represented as Vector embeddings in the same semantic space with the same dimensionality which allows us to compare all of these interchangeably for new use cases like searching image by text or searching videos to text or text to image or image to image whatever it may be one example might be generating product recommendations that not just that don't just look at your purchase history but maybe they also look at the Aesthetics and styles of the images that you've been searching so the following visualization shows we have a we have a graph here in three dimensions and you'll see these three axises uh living size and color now when I start to plot things on it I have an image of a cat and that uh we have a bunch of embeddings spotted on this three-dimensional chart we see the cat here but then I also put some text input a gray tabby cat laying in front of a Christmas tree and again we have the green uh the Green Dots here represent text uh in text that has turned into multimodal embeddings and you see that they both occupy a very similar space within this within this three-dimensional domain and this is not a coincidence right powerful embedding models have learned to represent the meaning behind text images video audio and more and because of this we can now classify uh we can classify we can search for images by text we can search you know videos by text text to image whatever it may be but we can classify multimedia content with impressive accuracy so the convergence of modalities here is what's really exciting for me uh and it opens up a ton of new applications for search and discovery of our data Note that this example up here is simplified we're only looking at three dimensions in reality our embeddings contain hundreds or thousands of Dimensions that represent or it's able to represent a lot more Nuance within our data so we're announcing this week the ability to generate these multimodal embeddings directly within big query with the familiar SQL syntax that you're used to you simply use the ml. Generate embedding function over an object table which points to objects in cloud storage again those could be that could be unstructured text it could be images audio whatever it may be and the embeddings are then returned directly to Big query the ml. Generate embedding function supports multiple embedding models to suit your use case again we're talking about multimodal but it also supports uh text embeddings as well as multilingual text embeddings uh and we can just contrast this to the way that you might generate embeddings right now which might be spinning up some python installing some libraries managing your API calls and your quotas wrangling some infrastructure to make it all happen and then finding a suitable place to actually store that data in some sort of vector database uh the point is that you can do all of this now in big query with the ease of SQL that you're used to so now that we've seen a little bit about multimodal embeddings and how they can occupy where they sit in a vector space the question becomes okay given an arbitrary embedding of something some sort how do I find what's nearby for example we see this red dot up here and we if we give the input text phrase gray tabby cat maybe that's the Red Dot on this on this three-dimensional space how do I find the closest matches to that whether that be text images video or something else and that's where Vector search comes in so at its most basic level Vector search the idea of vector search is to provide an input some sort of input content and then look through all of our catalog and find what's most you know what's closest to the input uh in this example we have here we have a some sort of rainbow sweater and then we search our Corpus of of items that could be your product image catalog you know it might have hats shoes shirts pants whatever it may be and then we we look for the most common or the the best uh the best matches and we rank them on the right side this ties in very closely to our last section on embeddings because behind the scenes uh all of this runs on embeddings right we turn this input image into an embedding everything in the middle in this embedding space is embeddings too and then we search we search for the closest embeddings so there's a bit of math that goes on to find the closest embedding to our input then we rank the results on the right side thanks to embeddings uh this type of search understands the.

---

## System Capabilities, Implementation Details, and Workload Optimization

Meaning behind our unstructured data again so this this image could find uh maybe polo shirts that are rainbow or I don't know rainbow whatever it may be that you have in your in your product catalog additionally we don't just have to input an image we could input a text that say chunky rainbow sweaters comfy colorful heavy you know heavyweight gear for the winter whatever it may be the point is we can use these synonyms and then we can get back the results that are most close closest to what we're putting in um we're able to return results that are similar in nature even if they don't share the same exact keywords the same pixels or the same frames within a video so one approach to Vector search to looking through this big Corpus of items is to compare the input against every item in our catalog this means calculating the distance between uh our input in every item and that is time consuming and compute intensive so it's not it's probably not the best use case when we're thinking about how do we scale out to millions or billions of Records so this is this approach brute force is usually better for smaller data sets or used for benchmarking purposes another approach is to use approximate nearest neighbor search in this case embeddings are turned embeddings in this space in the middle are classified into I'll just call them groups and you can see them kind of represented by these colors so we have like orange at the bottom pink on the left blue at the top whatever it may be and the idea is that when we run a search we only look at the group that's most like our our input so maybe that orange group down at the bottom is all sweaters or something like that so we're only looking at a subset of our data because when we have millions or billions of embeddings to look through it's just not efficient to look through all of them so this approach gives significantly faster results it's scalable to larger data sets but it still retains impressive recall because we have uh we have this index which kind of groups things for us now this week again we we're talking about Vector search right within big query this has been around for a little bit but uh multimodal support within Vector search for big query is what we're going to be focusing on so the idea is you could create a vector index on top of your embeddings in big B query and this index is fully maintained by big query so when your table your underlying table mutates or changes the index also changes automatically it's nothing that you need to worry about it's kind of the you know we have big query and everything is managed for you then you can run a vector search against this table at scale using your familiar SQL syntax additionally big query again Works across modalities so it works with multimodal embeddings some of the examples you made have seen in the last few weeks really focused on text but we're we're expanding to other modalities as well meaning that you can again unlock new use cases like searching image to text or text to image or video to text whatever it may be uh and lastly we're also adding Lang chain integration with big query to allow developers to build generative AI applications with ease so with that I'm going to pass things over to Martin to talk a little bit about architecture and go into the first demo hello let's continue by looking at the architecture of a sample integration so let's take step one and let's see what is the steps that you need to do in order to start with Vector search in big query first of all input data can take multiple forms it could be text image audio video and code itself step one is to encode these into vectors by using AI models called multimodel embedding models so it's multimodel embedding models these in order to search across semantics these embeddings are stored in bigquery for you to use next step two is to create a vector index on top of it in order to enable fast and scalable search step three searches the vector space based on input which could be something similar to a text input or something similar to an image input or any modalities that you are using for the model itself this step involves writing the select statement for text input modalities we can use parameterized queries and the special function built on top on top of the hql in order to transform into embeddings calling the remote AI model out of the box for you in the background let's go next SQL itself isn't design for image inputs in the case of image inputs modality the client application needs to fetch first the embeddings using any AI model on the market then this array of information can be inlined for the query itself finally let's talk a bit about the output itself in essence stept leverages the power of vector space to return information that can vary and can be any of the modalities can be used interchangeably to use cases like searching image by text or searching even video by text and any combination of these modalities let's switch to the demo and see how vqu embedding generation works and we will demo semantic search and we created an application in order to show you the multimodel lookup tool we have here the big query UI the first step to get started is to find a data set of images in order to use for that data is on class storage and we are able to find an object table we are able to create an object table on top of these objects in big query once the table is uh created we can check for example the elements for those that are new to this functionality we are able to get a table of pads sizes and different type of content type information we need to create a remote model in order to work with fetching the embeddings by using a remote connection we are addressing a multim modile embedding model which is on vertex AI this is familiar SQL Syntax for you and lets you enables you to have a model that you can interact with later once this model is created we are able to fetch the embeddings and to store in big query here we have the new generate embedding multimodal function which takes two inputs the model itself which we defined in the previous step and an object table in this situation this in the background lookups all the objects could be any type of file and it stores them beddings for you next if we look up for example the dimensions of this table in order to understand the context for our demo purposes there are 20 items and the length of the embedding is the 14 100 that we are used to nowadays if we are looking for looking up how this table added additional elements next to the object table details we will find generate embedding columns with the result the status it could be erroring out in case of permission issues for example that you need to care of now we have embedding stored in big query the next step is to enable Vector search in order to implement the use case in order to speed things up we can create a vector index what is a vector index Vector index enables you to create like a fast way to store the distance types and cosiness for your metrix in our demo we have few rows and this in this situation even the model itself tells you that you need to have at least 5,000 elements in order to create an Vector index in this situation it uses the brute force method for search essentially on a business use case you will have a lot more data in order to use for this example and finally step three is the query that we are going to run for the search itself when I run this query in the background it happens two step one is we are generating an ending for the input itself the input right now is dinour and this is a text input and we are using the same generate embedding function as previously and we are using the new Vector search function which TR three elements or three Fields the table where we stored the amb edings the column information where the embeddings are stalled in that table and the third one which sources the embedding input of uh the of the query of what we asked for we have here results that are similar to our input and we have even instances that we can use to order by the results or set a threshold on top of F let's go to uh demo application we have developed to understand visually better the concept we have chosen Google lmar data set we have uploaded 100,000 images in bigquery object table on Google class storage we created embeddings on top of it to store the embeddings is 1 gab and we have similar images as you see here depicted this step of the demo uses semantic search with text input using big quy Vector search we can have here simple prompts like show me a picture of a cat in the background we have the same two functionality that I shown on the sample itself first of all the input text is being transformed.

---

## Performance Scaling, Distributed Coordination, and Reliability

Into a embedding and then the search is being executed from that uh Common Table expression how I set up this separately in our our our demo itself what we see is similar images to our input show me a picture of a cat as the data it has different nuances we can try a demo like find images with two identical objects on it I find this very interesting because so far the technology wasn't able to establish quite nice this system if I run this demo we can imagine what two identical objects mean and it will return us different kind of pictures where we see duplicates or similar identical objects on the image itself let's move next and this step of the demo will use an image as an input and we want to look out for similar images I will use Castello brand from Romania which is often linked to Dracula in a way let's see if it finds similar images that depict uh this kind of input image here we have two steps first we are fetching embeddings using any AI model available on the market and we are getting back similar images to this uh query input itself so in this demo you have seen that generating embedding in bigquery is a familiar SQL syntax and you can run on top of your existing data on top of your existing object tables and you are able to see how Vector search combines in a text and image input modality but this can be added also for audio video and even code itself for it let's welcome back Jeff in order to show us other modalities and how to use in business use case good job awesome thank you Martin so moving past uh embedding or moving past this awesome demo um I just want to show a couple of architectures that we're seeing right now with with uh you know big query practitioners the first is well we've already gone through the first three steps right we have some raw data it could be in different types of modalities we generate embeddings on them they could be in you could use big query as we're showing today we could use vertex AI we could use other systems the point is we're generating embeddings and then we store them and we're storing them at Big query today um the the above example in blue is is what we're showing what Martin just showed and what we'll show today it's creating a vector index in big query and then it's serving those predictions from Big query as well uh the tradeoff there's a little bit of tradeoff between the blue and the green and that's what I want to get into uh the approach up top in blue is a little bit more batch or oriented it can take a ton of data and it can run these searches across you know big at Big query scale right you don't need to manage your indexes you don't need to manage any infrastructure it's using big query um behind the scenes however your results might come back in a couple of seconds another approach down below is to use vertex AI feature store and in this case what happens is your your embeddings that are stored in big query are synced automatically over to to feature store after which uh we can run online predictions and this is going to give you results back in Mill seconds which is great for real time like really re real-time use cases however the downside is that you need to manage a little bit of more infrastructure and it's a little costlier so these are kind of the two ways that we're seeing people start to uh store you know store and store and serve embeddings uh using big query right now now changing the topic a little bit Lang chain has become quickly become one of the most popular open source orchestration Frameworks for working with large language models and to provide big query users with tools to quickly build generative AI applications we're announcing Lang chain integration with big query and this will help you simplify your code right you don't need to focus on you can focus on the logic of your application rather than the low-level implementation of certain details this also enables you to get started quickly using retrieval augmented gener retrieval augmented generation uh Frameworks on top of your big query data which is what we'll show in the next demo so in this demo we're going to talk a little bit about a little bit about uh large language models versus retrieval augmented generation just show a quick example like why would we use rag in the first place uh and then we're going to configure and use our new Lang chain big query component uh using the big query Vector store and lastly we'll talk a little bit about how to or we'll show how to extend these applications to powerful question and answer chains so let's jump over to the demo and get started all right all right so just to uh set the stage here in this demo I'm going to be using a uh we're going to be thinking about cars or Automotive uh manuals today and I want to ask questions about my car so here is the symbol Starlight 2024 owners manual now the simple Starlite 2024 is not an actual car it is a Google uh madeup car that will be using just for today's example um but it is an owner's manual and it does contain a lot of information that you might see in other automobile uh Owner Manuals so going back over to Big query uh we will start in a big query notebook I've already run some setup like I connected to a runtime I imported a bunch of libraries so we don't have to go through any of that like not very exciting stuff we can skip right to the fun things um now most people here have probably had some experience with large language models I think we've all used them for some use case whether that be to debug your code or ask your boss how to say something differently whatever it may be um we many of us have experience with large language models but you might find that L llms don't always have the exact answer that you're looking for they might give you something that's General they might hallucinate they might just say I don't know Etc so I'll be using the new like as we just announced yesterday I think uh Gemini 1.5 pro model I'll import that and then I'm going to ask a question how many miles can I drive in the 2024 Google Starlite before I have to change my oil and when I run that on Gemini uh 1.5 Pro 1.5 I'm going to get some information that's like hey I don't have access to this I don't know what the 2024 Google Starlight is however here's some you know here's some additional tips that you might want to consider uh yeah contact Google support look at their website you know we don't have this car so it doesn't actually exist so this is not what we're looking for right if we just asked a straight large language model for something like this it's not going to give us an example and this is because the large language model was not trained on this data because it's not public data well it is public now but uh it wasn't public until a couple days ago uh this this this uh owner's manual so the idea here is we're going to move over to retrieval augmented generation and in doing so we'll begin with an ingestion pipeline um an ingestion pipeline I like to think of it as we have some input in this case we have a PDF we're going to ingest this document and we're going to split it into chunks um these chunks are going to help us with better recall and better and generate better results in the end so we take this big document maybe it's like 30 pages I don't know and we turn it into chunks we turn those chunks into embeddings and then we store them in a vector database this case it's going to be big query so we'll we'll talk about the ingestion part first now in order to again create these embeddings we need an embedding model and we'll use a text embedding model here from vertex AI um the text embedding gecko so we'll just say that and then we're going to ingest the PDF so I'll be using Lang chain uh Google Cloud Storage file loader I'm going to just sync the sync this PDF over to my cloud storage bucket and then I'll bring it into memory uh so that we can start using it now once that once that's finished we can verify that we did indeed I mean it says here we did load one document before we've chunked it but I can also just verify that uh I do have a one document name and it exists in my cloud storage bucket so if I went over to cloud storage we could check that out and it it is there next we need to split the data right we need to split this into chunks and we'll use Lang chain to do that too we'll use Lang chains uh recurs of character text splitter to do that and it should just take a second to do it it takes less than a second to run uh on this document and we've.

---

## Production Best Practices, Real-World Use Cases, and Next Steps

Turned this document now into 51 chunks now we need to turn those chunks into embeddings and get them over into bigquery so before we do that I'm going to first Define a data set in a table in big query where those chunks and their embeddings and any other metad datas going to be stored so I'll run that and then uh I'll just Define our Lang chain Vector store which takes inputs like the project ID data set table region Etc now I think this is this is kind of where some of the magic happens and this add documents uh against our doc splits and what's Happening Here is we're we're calling Upon Our Big BQ Vector cars manual or vector store that we just uh defined a second ago and it's taking all of those 51 chunks and it's now turning them into embeddings and then writing them directly to Big query so uh what was the table name Starlite data set auto manual table Starlite so if I look in here and I refresh this we should now have a table called Starlite and uh the details right I just created it or I just updated it and uh if we look at a preview of the data we can now see uh some information on it we see the chunks themselves we see the text embeddings and we see some metadata around each of those chunks um so let's move back to our our notebook and I'll just Begin by running a quick similarity search so this is just saying like what are the documents that are closest to this uh query again how many miles the same question that we asked of of the large language model how many miles can I drive in the Google Starlight until I need an oil change and the uh the results are kind of ugly right they're just pulling straight document chunks as well as the metadata uh the probably like the top five here and this is like we can we could probably parse something out of this like I don't know but it's it's a it's a little bit complicated to actually look through so in the next part of this demo we'll we'll talk about like this is the retrieval now we're going to talk about augmentation and generation so how does that actually work well i' like to talk about the Q&A pipeline again we did the ingestion part of this we took this and we put it all in our Vector database we took a PDF we turned it into embeddings put it in a vector database now the Q&A pipeline I like to think of it as I ask a question the question is turned into an embedding we run a vector search with that embedding against our database which is Big query and we retrieve the top results those are the document chunks they could be images they could be other things as well then those are sent to to the large language model as input to augment the large language model and then we generate an answer using those chunks and then we we return that to the uh The Smiling person here so let's just give that a shot um what I'm going to do is Define Gemini Pro as my large language model that does a summarization and we'll use just a really basic retrieval QA chain when I ask it again the same question how many miles can I drive in this car until I need an oil change we should get something back and we do so we see the query and we see the result we see 5,000 miles until I need to change my oil so this is great this is something that we didn't get when we asked the large language model just a minute ago because now we're looking at the source documents now I can also go a little bit deeper and talk about conversational retrieval chains so the idea here is that we persist the uh the answers in memory and then we can start to ask further questions so follow-up questions so here I'm going to ask a little bit of a different question does my symbol Starlight have roadside assistance the answer will be uh you know the the answer of this will be kept in memory uh and we can also access again our trunks so then I can ask what number do I call for assistance so we know that we're talking about the symbol Starlight what number do I call for that roadside assistance says I don't know so uh that's not the result we're looking for so we'll try it again um okay there we go 1 800 555 1212 so that that is what we're looking for and that was that is in the document itself so you know these systems are not perfect yet but they are getting better every day um we could then ask another question followup like what if I need assistance in a remote area area we'll see if it gives us a result here I don't know the answer you know as as we run this a few times we may get answers we may get different answers um it's usually pretty consistent but uh I guess for for the live demo it's uh it's giving us a little bit of different results so that that's where we are today on on this so the the idea though is that we can have conversations with this uh as we have as we input more chunks and more documents uh these conversational retrieval chains get better and better so if we uh you know we have one example here with I don't know it's maybe 20 pages of text or so if we are able to put in more and more we're probably going to get better search results as well now there's a lot more we could do in Lang chain but I just wanted to show you kind of uh an overview of what we're able to do right now uh with big query's integration with Lang chain the ability to get set up quickly build basic retrieval augmented generation applications on top of your data in Bitcoin quy um and just to go back recall that just a second ago when we used the large language model it just said I have no idea about this car CU it doesn't exist it wasn't trained on that but as we give it some more information and we augment it all of a sudden we're able to get some better uh some better answers so with that I'll move back to the presentation and all right so just talking about I just want to go over a couple quick use cases about what else we can do that's not just search um we could you know embeddings in Vector search allow a lot of different use cases like we could do some recommendations classification based on some sort of similarity um you could even use embeddings for something like your website's clickstream data right you know what a user a path a user takes throughout their website well maybe now I want to identify historical patterns or users kind of take the same path through the website the key is that embeddings is the key point is that embeddings and Vector search are flexible tools and they work with multimodal data so you can provide Solutions in a lot of different areas and the best part is that you can do this all within big query without having to go find other tools in order to do this so just some takeaways for this talk um the ml the new ml. Generate embedding function allows you to create multi M modal Vector embeddings right with and big query um it works with unstructured text images and video is coming very soon second Vector indexes allow you to create an index over your data for better search or for faster search and retrieval and ve uh Vector search function allows you to uh is great for analytical use cases that effectively process Vector search across a huge amount of data um you can start quickly right you can always continue to use the big query functionality that you're used to that you've come to expect like governance finding grain Access Control uh server list the ability to scale up and scale down right you don't need to have a vector store or vector database that's persistent that you have to worry about managing that you have to think about the infrastructure and all of that uh it's all managed for you in big query and lastly the new Lang chain implementation simplifies the process for python Integrations with open source and other third party Frameworks allowing you to get started quickly building gen applications on top of your data in big query oh shoot and with that this this is a page that I've put together that just has a lot of resources you can either scan the QR code or go to the link uh down here at the bottom uh this has documentation blogs videos uh and notebooks that you can check out like check check it all out uh you can click on any of these links this is a public slide that you can use um yeah check that out and let me know if you have any questions afterward on all of this so thank you all for attending I think we have a few uh we have a little bit of time for Q&A so we'll take those and anything else we can answer outside [Applause] afterward w.

---

## Source

Full cleaned transcript: `DATA/videos/unleash-the-power-of-vector-search-and-m-2024.json`
Original YouTube Video: https://youtu.be/B-0dZGJDtJw
